import { Key } from '../foundation/key';
import { StatelessWidget$ } from './framework';
declare class Spacer extends StatelessWidget$ {
    flex: number;
    constructor(namedParameters?: {
        key?: Key;
        flex?: number;
    });
}
export { Spacer };
