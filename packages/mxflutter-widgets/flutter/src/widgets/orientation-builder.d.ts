import { Key } from '../foundation/key';
import { StatelessWidget$ } from './framework';
declare class OrientationBuilder extends StatelessWidget$ {
    builder: any;
    constructor(namedParameters?: {
        key?: Key;
        builder?: any;
    });
}
export { OrientationBuilder };
